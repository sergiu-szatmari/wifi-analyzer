{
  "name": "wifi-analyzer",
  "version": "1.0.0",
  "description": "Wi-fi analyzer/sniffer app",
  "main": "src/main.ts",
  "scripts": {
    "build": "rm -rf build/ && tsc && clear",
    "sniffer": "npm run build && node ./build/main.js sniffer wlo1",
    "analyzer": "npm run build && node ./build/main.js analyzer wlo1mon"
  },
  "author": "Sergiu",
  "license": "ISC",
  "dependencies": {
    "cli-color": "^2.0.0",
    "pcap": "^3.1.0",
    "socket.io": "^3.0.3"
  },
  "devDependencies": {
    "@types/cli-color": "^2.0.0",
    "@types/node": "^14.14.7",
    "@types/socket.io": "^2.1.11",
    "tsc-watch": "^4.2.9",
    "typescript": "^4.0.5"
  }
}
