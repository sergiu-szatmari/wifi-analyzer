{
  "name": "wifi-analyzer",
  "version": "1.0.0",
  "description": "Wi-fi analyzer/sniffer app",
  "main": "src/app.ts",
  "scripts": {
    "build": "rm -rf build/ && tsc && clear",
    "sniffer": "npm run build && node ./build/app.js sniffer wlo1",
    "analyzer": "npm run build && node ./build/app.js analyzer wlo1mon"
  },
  "author": "Sergiu",
  "license": "ISC",
  "dependencies": {
    "cli-color": "^2.0.0",
    "pcap": "^3.1.0"
  },
  "devDependencies": {
    "@types/cli-color": "^2.0.0",
    "@types/node": "^14.14.7",
    "tsc-watch": "^4.2.9",
    "typescript": "^4.0.5"
  }
}
